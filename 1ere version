<!-- ResolvIA — Lidl IT · UTT Innovation Crunch Time -->
<!-- Criticité métier  <>  Responsabilité de résolution -->
<div id="app-root">
<div class="phone">
  <header class="topbar">
    <div class="brand">
      <div class="brand-mark"><span>L</span></div>
      <div class="brand-text">
        <strong>ResolvIA</strong>
        <em id="role-label">Espace magasin</em>
      </div>
    </div>
    <div class="net-pill" id="net-pill"><span class="dot"></span><span id="net-text">IA</span></div>
  </header>

  <main id="screen">

    <!-- ===== EMPLOYE ===== -->
    <section class="view" data-view="employee">
      <div class="hero">
        <div class="hero-eyebrow">Un incident en magasin&nbsp;?</div>
        <h1>Signalez,<br>l'IA priorise.</h1>
        <p>Décrivez le problème simplement. L'IA évalue l'impact métier et oriente vers le bon support.</p>
      </div>

      <div class="quick-grid" id="quick-grid"></div>

      <div class="composer">
        <label class="composer-label">Que se passe-t-il&nbsp;?</label>
        <textarea id="desc" rows="3" placeholder="Ex : Plus de réseau en SPM suite à une coupure de courant, les caisses sont offline…"></textarea>
        <div class="composer-meta">
          <div class="loc">
            <svg viewBox="0 0 24 24" class="ic"><path d="M12 21s-7-5.5-7-11a7 7 0 0 1 14 0c0 5.5-7 11-7 11Z"/><circle cx="12" cy="10" r="2.5"/></svg>
            <select id="store-loc">
              <option>SPM 4012 — Troyes — DR15</option>
              <option>SPM 4012 — Caisses</option>
              <option>SPM 4012 — Back-office / serveur</option>
              <option>SPM 4012 — Réserve / MOBI</option>
            </select>
          </div>
          <button class="btn-analyze" id="btn-analyze">
            <span class="ba-text">Analyser</span>
            <svg viewBox="0 0 24 24" class="ic"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </button>
        </div>
      </div>

      <div id="ai-result" class="ai-result hidden"></div>

      <div class="recent-block">
        <div class="block-head">Vos signalements</div>
        <div id="my-tickets" class="my-tickets"></div>
      </div>
    </section>

    <!-- ===== TECHNICIEN ===== -->
    <section class="view hidden" data-view="tech">
      <div class="tech-head">
        <div><h2>File support</h2><p id="tech-sub">Triée par criticité métier</p></div>
        <div class="seg" id="tech-filter">
          <button class="seg-btn active" data-f="open">Actifs</button>
          <button class="seg-btn" data-f="all">Tous</button>
          <button class="seg-btn" data-f="resolved">Clos</button>
        </div>
      </div>
      <div class="legend">
        <span><i class="sw crit"></i>Criticité métier = impact sur la vente</span>
        <span><i class="sw own"></i>Responsabilité = qui agit</span>
      </div>
      <div id="tech-list" class="tech-list"></div>
    </section>

    <!-- ===== DASHBOARD ===== -->
    <section class="view hidden" data-view="dash">
      <div class="dash-head"><h2>Pilotage IA</h2><p>Anticiper · Prioriser juste · Réduire l'interruption</p></div>
      <div class="kpi-grid" id="kpi-grid"></div>

      <div class="panel gap">
        <div class="panel-head"><span>Écarts de priorisation détectés</span></div>
        <p class="muted">Incidents où la priorité du système sous-évalue l'impact métier réel.</p>
        <div id="gap-list" class="gap-list"></div>
      </div>

      <div class="panel">
        <div class="panel-head"><span>Volume par équipement (cmdb_ci)</span></div>
        <div id="bars" class="bars"></div>
      </div>

      <div class="panel predict">
        <div class="panel-head"><span>Prédiction de pannes &amp; recommandations</span>
          <button class="btn-mini" id="btn-predict">Analyser</button></div>
        <div id="predict-body" class="predict-body">
          <p class="muted">L'IA croise la récurrence, les résolutions passées (base de connaissance) et les événements connexes (mises à jour, coupures) pour anticiper les prochaines pannes.</p>
        </div>
      </div>
    </section>
  </main>

  <nav class="tabbar">
    <button class="tab active" data-go="employee">
      <svg viewBox="0 0 24 24" class="ic"><path d="M4 19V9l8-5 8 5v10"/><path d="M9 19v-6h6v6"/></svg><span>Signaler</span></button>
    <button class="tab" data-go="tech">
      <svg viewBox="0 0 24 24" class="ic"><path d="m14 7 3 3"/><path d="M5 19 16 8l-3-3L2 16v3h3Z"/><path d="m18 6 1.5-1.5a2 2 0 0 0-3-3L15 3"/></svg><span>Support</span><em class="badge" id="tab-badge"></em></button>
    <button class="tab" data-go="dash">
      <svg viewBox="0 0 24 24" class="ic"><path d="M4 19V5M4 19h16"/><rect x="7" y="11" width="3" height="6"/><rect x="12" y="7" width="3" height="10"/><rect x="17" y="13" width="3" height="4"/></svg><span>Pilotage IA</span></button>
  </nav>
</div>
<div id="toast" class="toast"></div>
</div>

<style>
@import url('https://fonts.googleapis.com/css2?family=Archivo:wght@600;700;800;900&family=Hanken+Grotesk:wght@400;500;600;700&display=swap');
#app-root *{box-sizing:border-box;margin:0;padding:0}
#app-root{
  --blue:#0050aa;--blue-d:#003d82;--blue-l:#e9f0fb;
  --yellow:#ffe500;--red:#e3000f;--red-l:#fdebec;
  --ink:#0c1a2b;--ink-2:#5b6b7d;--line:#e4e9f0;--bg:#f4f6fa;--card:#fff;
  --ok:#0a8f5b;--ok-l:#e6f5ee;--amber:#c47a00;--amber-l:#fdf2dd;
  --own:#0e7490;--own-l:#e2f3f6;
  --r:16px;--shadow:0 1px 2px rgba(12,26,43,.06),0 8px 24px rgba(12,26,43,.07);
  font-family:'Hanken Grotesk',sans-serif;color:var(--ink);-webkit-font-smoothing:antialiased;line-height:1.45;
  display:flex;justify-content:center;align-items:flex-start;min-height:100vh;padding:18px 12px 28px;
  background:radial-gradient(900px 480px at 82% -12%,#dbe7fb 0,transparent 60%),radial-gradient(700px 460px at -10% 112%,#fff7c2 0,transparent 55%),var(--bg);
}
#app-root .phone{width:100%;max-width:440px;background:var(--bg);border-radius:30px;overflow:hidden;
  position:relative;display:flex;flex-direction:column;min-height:780px;
  box-shadow:0 30px 80px -20px rgba(12,26,43,.3);border:1px solid rgba(255,255,255,.6)}
.topbar{display:flex;align-items:center;justify-content:space-between;padding:18px 18px 14px;
  background:var(--card);border-bottom:1px solid var(--line);position:sticky;top:0;z-index:20}
.brand{display:flex;align-items:center;gap:11px}
.brand-mark{width:40px;height:40px;border-radius:11px;display:grid;place-items:center;position:relative;
  background:linear-gradient(135deg,var(--blue),var(--blue-d));box-shadow:0 6px 14px -4px rgba(0,80,170,.55)}
.brand-mark::after{content:"";position:absolute;inset:0;border-radius:11px;border:1.5px solid var(--yellow);opacity:.55}
.brand-mark span{font-family:'Archivo';font-weight:900;color:#fff;font-size:20px}
.brand-text{display:flex;flex-direction:column;line-height:1.1}
.brand-text strong{font-family:'Archivo';font-weight:800;font-size:18px;letter-spacing:-.02em}
.brand-text em{font-style:normal;font-size:11.5px;color:var(--ink-2);font-weight:500}
.net-pill{display:flex;align-items:center;gap:6px;font-size:11px;font-weight:700;background:var(--ok-l);
  color:var(--ok);padding:6px 10px;border-radius:999px}
.net-pill.off{background:var(--amber-l);color:var(--amber)}
.net-pill .dot{width:7px;height:7px;border-radius:50%;background:currentColor;animation:pulse 2s infinite}
@keyframes pulse{0%{box-shadow:0 0 0 0 rgba(10,143,91,.5)}70%{box-shadow:0 0 0 7px rgba(10,143,91,0)}100%{box-shadow:0 0 0 0 rgba(10,143,91,0)}}
main#screen{flex:1;overflow-y:auto;overflow-x:hidden;padding-bottom:92px}
.view{padding:20px 18px 10px;animation:fade .4s ease both}
.hidden{display:none!important}
@keyframes fade{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}
.ic{width:18px;height:18px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;flex:none}
.hero-eyebrow{font-size:12px;font-weight:700;color:var(--blue);text-transform:uppercase;letter-spacing:.08em;margin-bottom:8px}
.hero h1{font-family:'Archivo';font-weight:900;font-size:31px;line-height:1.05;letter-spacing:-.03em;margin-bottom:9px}
.hero p{color:var(--ink-2);font-size:14px;max-width:36ch;margin-bottom:18px}
.quick-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:16px}
.qcat{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:12px 6px;display:flex;
  flex-direction:column;align-items:center;gap:7px;cursor:pointer;transition:.18s;font:inherit}
.qcat:hover{transform:translateY(-2px);box-shadow:var(--shadow)}
.qcat.sel{border-color:var(--blue);background:var(--blue-l)}
.qcat .ic{width:21px;height:21px;color:var(--blue)}
.qcat span{font-size:10.5px;font-weight:600;text-align:center;color:var(--ink-2);line-height:1.15}
.qcat.sel span{color:var(--blue)}
.composer{background:var(--card);border:1px solid var(--line);border-radius:var(--r);padding:16px;box-shadow:var(--shadow)}
.composer-label{font-size:13px;font-weight:700;display:block;margin-bottom:9px}
#desc{width:100%;border:1px solid var(--line);border-radius:12px;padding:12px 13px;font:inherit;font-size:14.5px;
  resize:vertical;background:#fafbfd;transition:.15s}
#desc:focus{outline:none;border-color:var(--blue);background:#fff;box-shadow:0 0 0 4px rgba(0,80,170,.1)}
.composer-meta{display:flex;gap:10px;margin-top:12px}
.loc{flex:1;display:flex;align-items:center;gap:7px;background:#fafbfd;border:1px solid var(--line);
  border-radius:11px;padding:0 11px;color:var(--ink-2)}
.loc select{border:none;background:none;font:inherit;font-size:12px;font-weight:600;color:var(--ink-2);width:100%;padding:11px 0;outline:none}
.btn-analyze{display:flex;align-items:center;gap:8px;background:var(--blue);color:#fff;border:none;border-radius:11px;
  padding:0 18px;font:inherit;font-weight:700;font-size:13.5px;cursor:pointer;transition:.18s}
.btn-analyze:hover{background:var(--blue-d)}.btn-analyze:disabled{opacity:.6;cursor:wait}
.btn-analyze .ic{width:16px;height:16px}
.ai-result{margin-top:16px;animation:fade .4s ease both}
.ai-card{background:var(--card);border:1px solid var(--line);border-radius:var(--r);overflow:hidden;box-shadow:var(--shadow)}
.ai-top{padding:15px 16px;display:flex;align-items:flex-start;gap:12px;background:linear-gradient(180deg,var(--blue-l),#fff)}
.ai-spark{width:34px;height:34px;border-radius:10px;flex:none;display:grid;place-items:center;
  background:linear-gradient(135deg,var(--blue),#2a7de1)}
.ai-spark svg{width:18px;height:18px;color:#fff;fill:none;stroke:currentColor;stroke-width:2}
.ai-top h3{font-family:'Archivo';font-size:15px;font-weight:800;margin-bottom:2px}
.ai-top p{font-size:12px;color:var(--ink-2)}
.axes{display:grid;grid-template-columns:1fr 1fr;gap:10px;padding:14px 16px 4px}
.axis{border:1px solid var(--line);border-radius:12px;padding:11px 12px}
.axis .al{font-size:9.5px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--ink-2);
  display:flex;align-items:center;gap:5px;margin-bottom:7px}
.axis .al .ic{width:12px;height:12px}
.crit-pill{display:inline-flex;align-items:center;gap:6px;font-family:'Archivo';font-weight:800;font-size:15px;
  padding:5px 11px;border-radius:9px}
.crit-pill.P1{background:var(--red);color:#fff}
.crit-pill.P2{background:var(--amber);color:#fff}
.crit-pill.P3{background:var(--blue);color:#fff}
.crit-pill.P4{background:#9aa7b6;color:#fff}
.axis .sub{font-size:11px;color:var(--ink-2);margin-top:7px;line-height:1.4}
.axis.own{background:var(--own-l);border-color:#cce6ea}
.axis.own .al{color:var(--own)}
.own-tag{display:inline-flex;align-items:center;gap:6px;font-weight:800;font-size:12.5px;color:var(--own);
  background:#fff;border:1px solid #bfe2e7;padding:6px 10px;border-radius:9px}
.own-tag .ic{width:14px;height:14px}
.contrast{margin:12px 16px 2px;background:#fff8d6;border:1px solid #f3e08a;border-radius:11px;
  padding:10px 12px;font-size:12px;display:flex;align-items:flex-start;gap:9px}
.contrast b{color:var(--ink)}
.contrast .ic{width:15px;height:15px;color:var(--amber);margin-top:1px}
.tags{display:flex;flex-wrap:wrap;gap:7px;padding:12px 16px 2px}
.tag{font-size:11px;font-weight:700;padding:5px 10px;border-radius:8px;background:#eef2f7;color:var(--ink-2);
  display:flex;align-items:center;gap:5px}
.tag.ci{background:var(--blue-l);color:var(--blue)}
.ai-fix{padding:12px 16px}
.ai-fix h4{font-size:11.5px;font-weight:800;text-transform:uppercase;letter-spacing:.05em;color:var(--ink-2);
  margin-bottom:10px;display:flex;align-items:center;gap:7px}
.ai-fix h4 .ic{width:15px;height:15px;color:var(--ok)}
.steps{list-style:none;display:flex;flex-direction:column;gap:9px}
.steps li{display:flex;gap:11px;font-size:13.5px;align-items:flex-start}
.steps li b{flex:none;width:21px;height:21px;border-radius:7px;background:var(--blue);color:#fff;font-size:11px;
  display:grid;place-items:center;font-weight:800;margin-top:1px}
.ai-actions{display:flex;gap:9px;padding:4px 16px 16px}
.ai-actions button{flex:1;border-radius:11px;padding:12px;font:inherit;font-weight:700;font-size:13px;
  cursor:pointer;border:1px solid var(--line);background:#fff;color:var(--ink);transition:.16s}
.ai-actions .solved{background:var(--ok);color:#fff;border-color:var(--ok)}
.ai-actions .escalate{background:var(--blue);color:#fff;border-color:var(--blue)}
.ai-actions .escalate:hover{background:var(--blue-d)}
.ai-skeleton{padding:22px 16px;display:flex;flex-direction:column;gap:12px}
.sk{height:14px;border-radius:7px;background:linear-gradient(90deg,#eef1f6 25%,#e1e6ee 37%,#eef1f6 63%);
  background-size:400% 100%;animation:sh 1.3s infinite}
.sk.w40{width:40%}.sk.w70{width:70%}.sk.w90{width:90%}
@keyframes sh{0%{background-position:100% 0}100%{background-position:-100% 0}}
.ai-think{font-size:12.5px;color:var(--blue);font-weight:700;display:flex;align-items:center;gap:9px;padding:0 16px 4px}
.spin{width:15px;height:15px;border:2px solid var(--blue-l);border-top-color:var(--blue);border-radius:50%;animation:rot .7s linear infinite}
@keyframes rot{to{transform:rotate(360deg)}}
.recent-block{margin:22px 0 8px}
.block-head{font-size:12.5px;font-weight:800;color:var(--ink-2);margin-bottom:11px;text-transform:uppercase;letter-spacing:.05em}
.my-tickets{display:flex;flex-direction:column;gap:9px}
.mini-tk{background:var(--card);border:1px solid var(--line);border-radius:13px;padding:12px 14px;display:flex;align-items:center;gap:11px}
.mini-tk .pip{width:9px;height:9px;border-radius:50%;flex:none}
.mini-tk .info{flex:1;min-width:0}
.mini-tk .info strong{font-size:13px;font-weight:700;display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.mini-tk .info span{font-size:11.5px;color:var(--ink-2)}
.st{font-size:10.5px;font-weight:800;padding:4px 9px;border-radius:7px;flex:none}
.st.open{background:var(--blue-l);color:var(--blue)}.st.progress{background:var(--amber-l);color:var(--amber)}
.st.resolved{background:var(--ok-l);color:var(--ok)}
.empty{text-align:center;color:var(--ink-2);font-size:13px;padding:22px;background:var(--card);border:1px dashed var(--line);border-radius:13px}
.tech-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:12px;flex-wrap:wrap}
.tech-head h2,.dash-head h2{font-family:'Archivo';font-size:23px;font-weight:900;letter-spacing:-.02em}
.tech-head p,.dash-head p{font-size:12.5px;color:var(--ink-2);margin-top:3px}
.seg{display:flex;background:#e9edf3;border-radius:10px;padding:3px;gap:2px}
.seg-btn{border:none;background:none;font:inherit;font-size:11.5px;font-weight:700;color:var(--ink-2);
  padding:7px 11px;border-radius:8px;cursor:pointer;transition:.15s}
.seg-btn.active{background:#fff;color:var(--blue);box-shadow:0 1px 4px rgba(12,26,43,.1)}
.legend{display:flex;flex-wrap:wrap;gap:14px;font-size:11px;color:var(--ink-2);margin-bottom:14px}
.legend span{display:flex;align-items:center;gap:6px}
.sw{width:11px;height:11px;border-radius:3px;flex:none}
.sw.crit{background:var(--red)}.sw.own{background:var(--own)}
.tech-list{display:flex;flex-direction:column;gap:11px}
.tk{background:var(--card);border:1px solid var(--line);border-radius:15px;overflow:hidden;box-shadow:var(--shadow);display:flex}
.tk-rail{width:6px;flex:none}
.tk-rail.P1{background:var(--red)}.tk-rail.P2{background:var(--amber)}
.tk-rail.P3{background:var(--blue)}.tk-rail.P4{background:#9aa7b6}
.tk-body{padding:13px 15px;flex:1;min-width:0}
.tk-row{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:7px}
.tk-id{font-size:11px;font-weight:700;color:var(--ink-2);font-family:'Archivo'}
.tk-sla{font-size:10px;font-weight:800;padding:3px 8px;border-radius:6px;background:#eef2f7;color:var(--ink-2)}
.tk-sla.warn{background:var(--red-l);color:var(--red)}
.tk h4{font-size:14px;font-weight:700;margin-bottom:4px;line-height:1.3}
.tk .loc-line{font-size:11.5px;color:var(--ink-2);margin-bottom:9px}
.tk-axes{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:9px}
.cbadge{display:inline-flex;align-items:center;gap:6px;font-family:'Archivo';font-weight:800;font-size:12px;
  padding:5px 9px;border-radius:8px;color:#fff}
.cbadge.P1{background:var(--red)}.cbadge.P2{background:var(--amber)}
.cbadge.P3{background:var(--blue)}.cbadge.P4{background:#9aa7b6}
.cbadge small{font-weight:700;opacity:.85;font-family:'Hanken Grotesk';font-size:10px}
.obadge{display:inline-flex;align-items:center;gap:6px;font-weight:800;font-size:11.5px;color:var(--own);
  background:var(--own-l);border:1px solid #cce6ea;padding:5px 9px;border-radius:8px}
.obadge .ic{width:13px;height:13px}
.diffchip{font-size:10px;font-weight:800;background:#fff8d6;border:1px solid #f3e08a;color:#8a6d00;
  padding:4px 8px;border-radius:7px;display:inline-flex;align-items:center;gap:4px}
.tk-meta{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:10px}
.chip{font-size:10.5px;font-weight:600;background:#f1f4f8;color:var(--ink-2);padding:4px 8px;border-radius:7px;display:flex;align-items:center;gap:4px}
.chip.rec{background:var(--red-l);color:var(--red);font-weight:700}
.chip.dup{background:#ede9fb;color:#5b3fc4;font-weight:700}
.tk-ai{background:linear-gradient(180deg,var(--blue-l),#f7faff);border:1px solid #dce8f8;border-radius:11px;
  padding:10px 12px;font-size:12px;margin-bottom:10px}
.tk-ai .lab{font-size:9.5px;font-weight:800;color:var(--blue);text-transform:uppercase;letter-spacing:.06em;
  display:flex;align-items:center;gap:6px;margin-bottom:6px}
.tk-ai .lab svg{width:13px;height:13px;fill:none;stroke:currentColor;stroke-width:2}
.tk-ai b{color:var(--ink)}
.tk-act{display:flex;gap:8px}
.tk-act button{flex:1;border:1px solid var(--line);background:#fff;font:inherit;font-weight:700;font-size:12.5px;
  padding:10px;border-radius:10px;cursor:pointer;transition:.15s;color:var(--ink)}
.tk-act .res{background:var(--ok);color:#fff;border-color:var(--ok)}
.tk-act .prog{background:var(--blue);color:#fff;border-color:var(--blue)}
.tk-act button:hover{box-shadow:var(--shadow)}.tk.done{opacity:.7}
.dash-head{margin-bottom:18px}
.kpi-grid{display:grid;grid-template-columns:1fr 1fr;gap:11px;margin-bottom:16px}
.kpi{background:var(--card);border:1px solid var(--line);border-radius:15px;padding:15px;box-shadow:var(--shadow);position:relative;overflow:hidden}
.kpi::after{content:"";position:absolute;right:-18px;top:-18px;width:62px;height:62px;border-radius:50%;background:var(--blue-l);opacity:.6}
.kpi .kv{font-family:'Archivo';font-size:26px;font-weight:900;letter-spacing:-.02em;position:relative;line-height:1}
.kpi .kl{font-size:11.5px;color:var(--ink-2);font-weight:600;margin-top:6px;position:relative}
.kpi .kt{font-size:10.5px;font-weight:800;margin-top:8px;display:inline-flex;align-items:center;gap:4px;position:relative}
.kt.up{color:var(--ok)}.kt.down{color:var(--red)}
.panel{background:var(--card);border:1px solid var(--line);border-radius:15px;padding:16px;box-shadow:var(--shadow);margin-bottom:14px}
.panel-head{display:flex;align-items:center;justify-content:space-between;font-size:13px;font-weight:800;margin-bottom:13px}
.panel .muted{color:var(--ink-2);font-size:12px;margin-bottom:11px}
.btn-mini{border:1px solid var(--blue);background:var(--blue);color:#fff;font:inherit;font-weight:700;font-size:11.5px;
  padding:7px 14px;border-radius:9px;cursor:pointer;transition:.15s}
.btn-mini:hover{background:var(--blue-d)}.btn-mini:disabled{opacity:.6;cursor:wait}
.gap-list{display:flex;flex-direction:column;gap:9px}
.gap-row{border:1px solid var(--line);border-radius:11px;padding:11px 12px;display:flex;align-items:center;gap:11px}
.gap-row .gx{flex:1;min-width:0}
.gap-row .gx strong{font-size:12.5px;display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.gap-row .gx span{font-size:11px;color:var(--ink-2)}
.prio-move{display:flex;align-items:center;gap:6px;font-family:'Archivo';font-weight:800;font-size:11px;flex:none}
.p-old{background:#eef2f7;color:var(--ink-2);padding:3px 7px;border-radius:6px}
.p-new{background:var(--red);color:#fff;padding:3px 7px;border-radius:6px}
.bars{display:flex;flex-direction:column;gap:11px}
.bar-row{display:flex;align-items:center;gap:11px;font-size:12px}
.bar-row .bn{width:108px;font-weight:600;color:var(--ink-2);flex:none;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.bar-track{flex:1;height:10px;background:#eef1f6;border-radius:6px;overflow:hidden}
.bar-fill{height:100%;border-radius:6px;background:linear-gradient(90deg,var(--blue),#3a86e0);width:0;transition:width 1s cubic-bezier(.2,.8,.2,1)}
.bar-row .bv{width:22px;text-align:right;font-weight:800;font-family:'Archivo';flex:none}
.predict-body{font-size:13px}
.risk{border:1px solid var(--line);border-radius:12px;padding:13px;margin-top:10px;display:flex;gap:12px;animation:fade .4s ease both}
.risk .rl{width:5px;border-radius:4px;flex:none}
.risk.high .rl{background:var(--red)}.risk.med .rl{background:var(--amber)}.risk.low .rl{background:var(--ok)}
.risk .rt{font-weight:800;font-size:13px;display:flex;justify-content:space-between;gap:8px;margin-bottom:5px}
.risk .rt em{font-style:normal;font-size:10px;font-weight:800;padding:3px 8px;border-radius:6px}
.risk.high .rt em{background:var(--red-l);color:var(--red)}.risk.med .rt em{background:var(--amber-l);color:var(--amber)}
.risk.low .rt em{background:var(--ok-l);color:var(--ok)}
.risk .rd{font-size:12px;color:var(--ink-2);line-height:1.5}.risk .rd b{color:var(--ink)}
.tabbar{position:absolute;bottom:0;left:0;right:0;display:flex;background:var(--card);border-top:1px solid var(--line);
  padding:9px 8px calc(9px + env(safe-area-inset-bottom));z-index:20}
.tab{flex:1;border:none;background:none;font:inherit;display:flex;flex-direction:column;align-items:center;gap:4px;
  padding:7px 0;color:var(--ink-2);cursor:pointer;position:relative;transition:.15s;border-radius:10px}
.tab .ic{width:21px;height:21px}.tab span{font-size:10.5px;font-weight:700}.tab.active{color:var(--blue)}
.tab .badge{position:absolute;top:2px;right:50%;transform:translateX(20px);background:var(--red);color:#fff;
  font-size:9.5px;font-weight:800;min-width:17px;height:17px;border-radius:9px;display:none;align-items:center;justify-content:center;padding:0 4px}
.tab .badge.show{display:flex}
.toast{position:fixed;left:50%;bottom:30px;transform:translate(-50%,80px);background:var(--ink);color:#fff;
  padding:13px 20px;border-radius:13px;font-size:13px;font-weight:600;box-shadow:0 14px 30px rgba(12,26,43,.35);
  opacity:0;transition:.35s cubic-bezier(.2,.9,.2,1);z-index:50;display:flex;align-items:center;gap:9px;max-width:88%}
.toast.show{transform:translate(-50%,0);opacity:1}
.toast .ic{width:17px;height:17px;color:var(--yellow)}
</style>

<script>
(function(){
  "use strict";
  var $=function(s){return document.querySelector(s)};
  var $$=function(s){return Array.prototype.slice.call(document.querySelectorAll(s))};
  var AP=String.fromCharCode(39);

  var CATS=[
    {id:'caisse', ci:'Checkout - Standard VT [LDL - FR]', n:'Caisse / KS', icon:'<path d="M3 9h18v11H3z"/><path d="M7 9V5h10v4"/><path d="M7 14h2m4 0h4"/>'},
    {id:'bo', ci:'ST-BO Server', n:'Serveur BO / SPM', icon:'<rect x="4" y="3" width="16" height="7" rx="1"/><rect x="4" y="14" width="16" height="7" rx="1"/><path d="M8 6.5h.01M8 17.5h.01"/>'},
    {id:'mobi', ci:'ST-MDE', n:'MOBI / Mobile Office', icon:'<rect x="7" y="2" width="10" height="20" rx="2"/><path d="M11 18h2"/>'},
    {id:'wifi', ci:'ST-Customer WLAN Hardware', n:'Wifi client', icon:'<path d="M5 13a10 10 0 0 1 14 0"/><path d="M8.5 16.5a5 5 0 0 1 7 0"/><circle cx="12" cy="20" r="1"/>'},
    {id:'reseau', ci:'ST-Network', n:'Réseau / Internet', icon:'<rect x="3" y="14" width="18" height="6" rx="1"/><path d="M7 17h.01M12 14V8M12 8a3 3 0 1 0-3-3"/>'},
    {id:'imprimante', ci:'ST-Printer', n:'Imprimante ticket', icon:'<path d="M6 9V3h12v6"/><rect x="4" y="9" width="16" height="8" rx="1"/><path d="M8 17h8v4H8z"/>'}
  ];
  var catName=function(id){var c=CATS.filter(function(x){return x.id===id})[0];return c?c.n:'Autre'};
  var catCI=function(id){var c=CATS.filter(function(x){return x.id===id})[0];return c?c.ci:'ST-Other'};

  var OWNERS={
    n1:{n:'Support N1 — Hotline',ic:'<path d="M3 5a2 2 0 0 1 2-2h2l2 4-2 1a12 12 0 0 0 6 6l1-2 4 2v2a2 2 0 0 1-2 2A16 16 0 0 1 3 5Z"/>'},
    n2:{n:'Support N2 / Infra',ic:'<path d="m14 7 3 3M5 19 16 8l-3-3L2 16v3h3Z"/>'},
    ext:{n:'Maintenance externe (site)',ic:'<path d="M21 16V8a2 2 0 0 0-1-1.7l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.7l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z"/>'},
    elec:{n:'Électricien — Non-IT',ic:'<path d="M13 2 3 14h7l-1 8 10-12h-7l1-8Z"/>'},
    dr:{n:'DR / RVS — Non-IT',ic:'<circle cx="12" cy="8" r="4"/><path d="M4 21a8 8 0 0 1 16 0"/>'}
  };
  var ownName=function(o){return (OWNERS[o]||OWNERS.n1).n};

  var KB={
    caisse:['Faites un reboot électrique de la caisse (éteindre 10 s, rallumer).','Vérifiez le câble réseau RJ45 derrière l'+AP+'UC de la caisse.','Patientez le rechargement du TPOS, puis rouvrez la caisse.'],
    bo:['NE coupez PAS le serveur si le SPM est offline.','Vérifiez que les baies sont alimentées (voyants) et le disjoncteur du TGBT.','Notez l'+AP+'heure exacte de début et attendez la consigne de reboot STS.'],
    mobi:['Relancez l'+AP+'application via EMS Launcher sur le MOBI.','Faites un Reset Client de l'+AP+'application (FWWS).','Purge statique : retirez la batterie 10 s, remettez, laissez charger.'],
    wifi:['Redémarrez le routeur SFR (vérifiez les voyants Power / Internet).','Testez sur Android ET iOS pour cibler la panne.','Oubliez le réseau Wifi puis reconnectez-vous.'],
    reseau:['Vérifiez les voyants des routeurs opérateur (vert fixe).','Redémarrez la box / le routeur concerné.','Testez sur un autre poste pour isoler la panne.'],
    imprimante:['Ouvrez le capot et retirez le papier coincé.','Réinsérez correctement le rouleau de papier.','Relancez une impression test.']
  };

  var NOW=Date.now(),H=3600000;
  var SEED=[
    {id:'INC13210111',ci:'bo',store:'1549',city:'SAINT POL SUR MER',dr:'DR13',
     short:'Plus de réseau en SPM suite à coupure de courant',
     desc:'Plus de Wifi ni de réseau dans le SPM suite à une coupure de courant. Caisses offline.',
     snPrio:'P3',crit:'P1',critReason:'Magasin entièrement à l'+AP+'arrêt : encaissement impossible, perte de CA directe.',
     owner:'elec',status:'progress',close:'',sla:NOW+1*H,rec:1,dup:null,t:NOW-2*H,
     ai:'Coupure électrique → STS / VM INF offline. Geste IT : reboot STS après retour courant. Cause hors IT (électricien) mais criticité métier maximale → escalade + communication magasin.'},
    {id:'INC13188915',ci:'caisse',store:'478',city:'EU',dr:'DR13',
     short:'KS2 et KS4 offline',
     desc:'Les caisses 2 et 4 sont offline. Câble réseau vérifié, redémarrage x2, toujours pas de réseau.',
     snPrio:'P2',crit:'P1',critReason:'Plusieurs caisses HS en simultané : files d'+AP+'attente, capacité d'+AP+'encaissement fortement réduite.',
     owner:'n2',status:'open',close:'',sla:NOW+3*H,rec:2,dup:null,t:NOW-3*H,
     ai:'Corrélé à perte réseau back-office. KB : vérifier switch caisses + câble derrière UC, sinon escalade N2 / inter technicien.'},
    {id:'INC13202881',ci:'caisse',store:'4314',city:'SAINT RENAN',dr:'DR15',
     short:'KS3 — chèque non garanti récurrent',
     desc:'Le chèque ne passe pas, message « non garanti » sur plusieurs chèques en caisse 3.',
     snPrio:'P2',crit:'P2',critReason:'Une caisse partiellement bloquée sur le paiement chèque : gêne l'+AP+'encaissement.',
     owner:'n1',status:'progress',close:'',sla:NOW+5*H,rec:3,dup:null,t:NOW-6*H,
     ai:'Logs TPE « NON GARANTI » récurrents. KB : test croisé sur une autre caisse, vérifier liaison TPE, surveiller récurrence.'},
    {id:'INC13182943',ci:'caisse',store:'3270',city:'SAINT MARCEL',dr:'DR11',
     short:'KS6 — GK application not running (alerte Nagios)',
     desc:'Alerte monitoring Nagios : CRITICAL - GK application not running sur la caisse 6.',
     snPrio:'P3',crit:'P2',critReason:'1 caisse hors service détectée automatiquement : à traiter avant ouverture pour ne pas réduire la capacité.',
     owner:'n1',status:'resolved',close:'Software / Application Issue',sla:NOW-20*H,rec:2,dup:null,t:NOW-28*H,
     ai:'Ingestion auto depuis Nagios. KB : reboot caisse → service GK redémarre → caisse online. Résolution type éprouvée.'},
    {id:'INC13209906',ci:'bo',store:'1687',city:'ST-GEORGES D ORQUES',dr:'DR16',
     short:'Coupure BO — VM offline',
     desc:'Coupure BO, VM offline, escalade siège demandée.',
     snPrio:'P1',crit:'P1',critReason:'Back-office indisponible : caisses offline, magasin à l'+AP+'arrêt.',
     owner:'n2',status:'resolved',close:'Software / Application Issue',sla:NOW-40*H,rec:1,dup:null,t:NOW-46*H,
     ai:'Base de données fermée → restart DBS. KB : redémarrage VM DBS/BOS rétablit le BO. Résolu, happy call OK.'},
    {id:'INC13217489',ci:'mobi',store:'3553',city:'LA CIOTAT',dr:'DR08',
     short:'MOBI — error initializing local persistance (FWWS)',
     desc:'Aucun accès à l'+AP+'application FWWS, erreur « initializing local persistence ».',
     snPrio:'P3',crit:'P3',critReason:'1 MOBI applicatif HS, le magasin reste opérationnel : gêne modérée.',
     owner:'n1',status:'resolved',close:'Software / Application Issue',sla:NOW-60*H,rec:5,dup:null,t:NOW-70*H,
     ai:'Erreur persistance FWWS très récurrente. KB : Reset Client → résolu en < 5 min. Candidat automatisation (geste self-service).'},
    {id:'INC13212552',ci:'mobi',store:'2967',city:'CORPEAU',dr:'DR12',
     short:'MOBI — batterie gonflée',
     desc:'La batterie a gonflé, le MOBI ne tient plus la charge.',
     snPrio:'P3',crit:'P3',critReason:'Matériel à remplacer, pas d'+AP+'arrêt de vente : impact modéré mais sécurité batterie à traiter.',
     owner:'ext',status:'open',close:'',sla:NOW+30*H,rec:1,dup:null,t:NOW-8*H,
     ai:'Diagnostic confirmé. KB : swap MOBI (expédition avec échange). Hors geste support → maintenance externe sur site.'},
    {id:'INC13215376',ci:'mobi',store:'3315',city:'LIVRY GARGAN',dr:'DR06',
     short:'MOBI perdu',
     desc:'Perte d'+AP+'un MOBI en magasin.',
     snPrio:'P3',crit:'P4',critReason:'Aucun impact sur la vente : demande administrative, pas un incident IT.',
     owner:'dr',status:'resolved',close:'Non-IT Reason',sla:NOW-50*H,rec:1,dup:'INC13215423',t:NOW-52*H,
     ai:'Hors périmètre IT : perte matériel → redirection RVS / DR (escalade.itmag). Doublon détecté avec INC13215423.'},
    {id:'INC13205893',ci:'wifi',store:'3150',city:'ST ESTEVE',dr:'DR22',
     short:'Wifi client — connecté mais pas d'+AP+'accès internet',
     desc:'Le réseau apparaît, on s'+AP+'y connecte, mais aucune connexion internet (tout le SPM).',
     snPrio:'P2',crit:'P2',critReason:'Service client dégradé (Wifi gratuit), pas d'+AP+'arrêt de vente : impact modéré à élevé.',
     owner:'n2',status:'resolved',close:'Resolved by Workaround',sla:NOW-30*H,rec:2,dup:null,t:NOW-34*H,
     ai:'Routeur SFR remonte offline. KB : ajout IP publique / whitelist (workaround Cloudi-fi). Résolution type connue.'},
    {id:'INC13153461',ci:'wifi',store:'1077',city:'PARIS',dr:'DR10',
     short:'Wifi instable / zone non couverte — récurrent',
     desc:'Absence de Wifi et réseau dans certaines zones, problème depuis plusieurs semaines.',
     snPrio:'P2',crit:'P2',critReason:'Récurrence longue, gêne badgeage/outils en zones non couvertes : impact opérationnel soutenu.',
     owner:'n2',status:'open',close:'',sla:NOW+2*H,rec:4,dup:null,t:NOW-5*H,
     ai:'Récurrent. KB : nécessite plan d'+AP+'implantation des bornes pour analyse couverture → escalade infra réseau.'},
    {id:'INC13164473',ci:'reseau',store:'2040',city:'SAVERDUN',dr:'DR14',
     short:'Coupure réseau totale en SPM',
     desc:'Plus aucun réseau en SPM. Check routeur OK, check élec OK, débranchement STS 15 min.',
     snPrio:'P1',crit:'P1',critReason:'Coupure réseau totale : caisses, BO et MOBI hors service simultanément. Vente impossible.',
     owner:'n2',status:'resolved',close:'Works as designed',sla:NOW-90*H,rec:1,dup:null,t:NOW-96*H,
     ai:'Réarmement disjoncteurs + reboot routeurs opérateur. KB : check baie + alimentation avant reboot réseau.'},
    {id:'INC13030001',ci:'imprimante',store:'2711',city:'QUISSAC',dr:'DR16',
     short:'Imprimante ticket KS1 — code-barres incomplet (récurrent)',
     desc:'L'+AP+'imprimante n'+AP+'imprime pas le code-barres en entier, ce qui empêche les retours.',
     snPrio:'P3',crit:'P3',critReason:'1 caisse impactée sur les retours uniquement : gêne modérée mais récurrente x4.',
     owner:'ext',status:'open',close:'',sla:NOW+22*H,rec:4,dup:null,t:NOW-9*H,
     ai:'Imprimante défectueuse, récurrent x4. KB : changement imprimante (intervention sur site) → préconisé, ne plus temporiser.'}
  ];

  var MEM=null,KEY='resolvia:v2';
  function load(){
    return Promise.resolve().then(function(){
      if(typeof window.storage==='undefined') throw 0;
      return window.storage.get(KEY);
    }).then(function(r){
      if(r&&r.value){MEM=JSON.parse(r.value);return}
      MEM=SEED.slice();save();
    }).catch(function(){MEM=MEM||SEED.slice();save()});
  }
  function save(){try{if(typeof window.storage!=='undefined')window.storage.set(KEY,JSON.stringify(MEM));}catch(e){}}

  var AI_ON=true;

  function localAnalyze(desc,hint){
    var d=(desc||'').toLowerCase();
    var ci=hint||'caisse';
    if(!hint){
      if(/cais|ks\d|scan|tpos|gk applic|chèque|cheque|tiroir/.test(d))ci='caisse';
      else if(/spm offline|bo offline|store ?serv|\bsts\b|vm (inf|dbs|bos)|coupure bo|back.?office/.test(d))ci='bo';
      else if(/mobi|fwws|persistance|persistence|ems launcher|pda/.test(d))ci='mobi';
      else if(/wifi client|wifi du client|hotspot|portail captif|cgu|page web indispo/.test(d))ci='wifi';
      else if(/imprim|ticket|papier|code.?barre|bourrage/.test(d))ci='imprimante';
      else if(/réseau|reseau|internet|coupure réseau|routeur|sfr|barracuda/.test(d))ci='reseau';
      else ci='caisse';
    }
    var crit='P3',reason='Gêne sans arrêt de vente : à traiter rapidement.';
    if(/coupure (de )?courant|coupure électr|toutes? les cais|plusieurs cais|spm offline|bo offline|coupure réseau totale|magasin (à l|a l).?arr|chaine du froid|chaîne du froid|tout le (spm|magasin) (offline|hs)/.test(d)){
      crit='P1';reason='Le magasin ne peut plus vendre : impact CA direct.';
    } else if(/cais|ks\d|tpe|paiement|chèque|cheque|wifi client|encaiss|bo |back.?office/.test(d)){
      crit='P2';reason='Fonction commerciale partiellement bloquée.';
    } else if(/perdu|volé|vol |perte mobi|cosmét|demande|doublon/.test(d)){
      crit='P4';reason='Aucun impact sur la vente.';
    }
    var owner='n1';
    if(/coupure (de )?courant|électric|disjoncteur|onduleur|tgbt/.test(d))owner='elec';
    else if(/perdu|volé|vol |hors périmètre|hors scope|non-it/.test(d))owner='dr';
    else if(/swap|écran cassé|batterie gonfl|remplac|changement (imprimante|uc|matériel)|expédition/.test(d))owner='ext';
    else if(/spm offline|bo offline|vm |store ?serv|coupure réseau|routeur|barracuda|infra|escalade n2/.test(d))owner='n2';
    return {ci:ci,crit:crit,crit_reason:reason,sn_prio:'P3',owner:owner,
      summary:'Analyse heuristique locale (IA hors ligne).',
      self_help:KB[ci]||KB.caisse,close_code:'Software / Application Issue',
      eta_minutes:crit==='P1'?20:crit==='P2'?45:120,escalate:(owner!=='n1')};
  }

  function aiAnalyze(desc,hint){
    var p='Tu es l'+AP+'IA support IT de Lidl (magasins/SPM). Analyse un incident remonté par un employé. '+
      'Tu DOIS séparer deux notions distinctes : '+
      '(A) la CRITICITE METIER = impact réel sur la capacité du magasin à vendre (indépendante de la catégorie et de qui répare) ; '+
      '(B) la RESPONSABILITE DE RESOLUTION = qui doit agir. '+
      'Une coupure de courant qui met le magasin à l'+AP+'arrêt est CRITICITE P1 même si la résolution revient à un électricien (Non-IT). '+
      'Description: """'+desc+'""". '+
      (hint?('Catégorie suggérée par employé: '+catName(hint)+'. '):'')+
      'cmdb_ci possibles (id): caisse, bo, mobi, wifi, reseau, imprimante. '+
      'owner possibles (id): n1 (hotline, gestes simples), n2 (infra/serveur/réseau), ext (maintenance externe sur site/swap), elec (électricien Non-IT), dr (DR/RVS Non-IT, perte/vol). '+
      'sn_prio = priorité que prédirait l'+AP+'ancien système ServiceNow (calée sur la catégorie, sous-évalue les coupures/arrêts totaux : tend vers P3). '+
      'crit = ta criticité métier réelle: P1 magasin ne peut plus vendre / chaîne du froid / sécurité ; P2 fonction commerciale partiellement bloquée ; P3 gêne sans arrêt de vente ; P4 aucun impact. '+
      'Réponds UNIQUEMENT en JSON strict, sans texte ni balises: '+
      '{"ci":"<id>","crit":"P1|P2|P3|P4","crit_reason":"<1 phrase impact métier>","sn_prio":"P1|P2|P3","owner":"<id>","summary":"<diagnostic court>","self_help":["<geste 1>","<geste 2>","<geste 3>"],"close_code":"<close code probable>","eta_minutes":<entier>,"escalate":true|false}';
    return fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:{"Content-Type":"application/json"},
      body:JSON.stringify({model:"claude-sonnet-4-20250514",max_tokens:1000,messages:[{role:"user",content:p}]})
    }).then(function(r){if(!r.ok)throw new Error('api');return r.json()}).then(function(data){
      var t=(data.content||[]).filter(function(b){return b.type==='text'}).map(function(b){return b.text}).join('');
      var o=JSON.parse(t.replace(/```json|```/g,'').trim());
      if(!o.ci||!o.crit)throw new Error('json');return o;
    });
  }
  function analyze(desc,hint){
    return aiAnalyze(desc,hint).then(function(r){AI_ON=true;netUI();return r;})
      .catch(function(){AI_ON=false;netUI();return localAnalyze(desc,hint);});
  }

  function aiPredict(){
    var hist=MEM.map(function(t){return {ci:t.ci,short:t.short,crit:t.crit,snPrio:t.snPrio,status:t.status,rec:t.rec}});
    var p='Tu es l'+AP+'IA de pilotage IT de Lidl. Historique d'+AP+'incidents (JSON): '+JSON.stringify(hist)+
      '. En croisant la récurrence (rec), les cmdb_ci et la criticité, identifie les 3 risques de panne les plus probables à anticiper. '+
      'Réponds UNIQUEMENT en JSON: {"risks":[{"equipment":"<équipement>","level":"high|med|low","probability":"<ex 78%>","reason":"<court>","action":"<action préventive>"}]}';
    return fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:{"Content-Type":"application/json"},
      body:JSON.stringify({model:"claude-sonnet-4-20250514",max_tokens:1000,messages:[{role:"user",content:p}]})
    }).then(function(r){if(!r.ok)throw new Error('api');return r.json()}).then(function(data){
      var t=(data.content||[]).filter(function(b){return b.type==='text'}).map(function(b){return b.text}).join('');
      return JSON.parse(t.replace(/```json|```/g,'').trim());
    });
  }
  function localPredict(){return {risks:[
    {equipment:'MOBI — application FWWS (parc)',level:'high',probability:'84%',reason:'Erreur « local persistence » très récurrente sur plusieurs SPM.',action:'Déployer le Reset Client en self-service guidé pour supprimer ces tickets.'},
    {equipment:'Imprimante ticket — Caisse 1 (SPM 2711)',level:'high',probability:'76%',reason:'Code-barres incomplet, récurrent x4, matériel dégradé.',action:'Planifier le remplacement avant rupture, ne plus temporiser.'},
    {equipment:'Wifi client — SPM à récurrence élevée',level:'med',probability:'58%',reason:'Réouvertures multiples liées à la couverture des bornes.',action:'Collecter le plan d'+AP+'implantation et lancer une étude couverture.'}
  ]};}

  function pip(p){return p==='P1'?'var(--red)':p==='P2'?'var(--amber)':p==='P3'?'var(--blue)':'#9aa7b6'}
  function toast(m){var t=$('#toast');t.innerHTML='<svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>'+m;t.classList.add('show');clearTimeout(t._tm);t._tm=setTimeout(function(){t.classList.remove('show')},3400)}
  function netUI(){var p=$('#net-pill');p.classList.toggle('off',!AI_ON);$('#net-text').textContent=AI_ON?'IA active':'IA locale'}
  function ago(t){var m=Math.round((Date.now()-t)/60000);if(m<60)return 'il y a '+m+' min';var h=Math.round(m/60);if(h<24)return 'il y a '+h+' h';return 'il y a '+Math.round(h/24)+' j'}
  function slaTxt(t){var m=Math.round((t-Date.now())/60000);if(m<=0)return {t:'SLA dépassé',w:true};if(m<60)return {t:'SLA '+m+' min',w:m<45};var h=Math.round(m/60);return {t:'SLA '+h+' h',w:h<2}}
  function ownIcon(o){return '<svg class="ic" viewBox="0 0 24 24">'+(OWNERS[o]||OWNERS.n1).ic+'</svg>'}

  var selCat=null;
  function renderCats(){
    $('#quick-grid').innerHTML=CATS.map(function(c){
      return '<button class="qcat" data-c="'+c.id+'"><svg class="ic" viewBox="0 0 24 24">'+c.icon+'</svg><span>'+c.n+'</span></button>';
    }).join('');
    $$('.qcat').forEach(function(b){b.onclick=function(){
      var id=b.dataset.c;selCat=selCat===id?null:id;
      $$('.qcat').forEach(function(x){x.classList.toggle('sel',x.dataset.c===selCat)});
    }});
  }

  function renderMine(){
    var mine=MEM.filter(function(t){return t.mine}).slice().reverse();
    var el=$('#my-tickets');
    if(!mine.length){el.innerHTML='<div class="empty">Aucun signalement pour le moment.</div>';return}
    el.innerHTML=mine.map(function(t){
      return '<div class="mini-tk"><span class="pip" style="background:'+pip(t.crit)+'"></span>'+
        '<div class="info"><strong>'+t.short+'</strong><span>'+t.id+' · '+catName(t.ci)+' · '+ago(t.t)+'</span></div>'+
        '<span class="st '+t.status+'">'+(t.status==='open'?'Ouvert':t.status==='progress'?'En cours':'Résolu')+'</span></div>';
    }).join('');
  }

  var last=null,lastDesc='',lastLoc='';
  function showSkeleton(){
    var r=$('#ai-result');r.classList.remove('hidden');
    r.innerHTML='<div class="ai-card"><div class="ai-think"><span class="spin"></span>L'+AP+'IA évalue l'+AP+'impact métier…</div>'+
      '<div class="ai-skeleton"><div class="sk w40"></div><div class="sk w90"></div><div class="sk w70"></div><div class="sk"></div><div class="sk w70"></div></div></div>';
    r.scrollIntoView({behavior:'smooth',block:'nearest'});
  }
  function showAnalysis(a){
    last=a;
    var diff = a.sn_prio && a.sn_prio!==a.crit;
    var contrast = diff ? '<div class="contrast"><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 9v4M12 17h.01M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0Z"/></svg>'+
      '<div>Le système (ServiceNow) aurait classé <b>'+a.sn_prio+'</b>. ResolvIA évalue <b>'+a.crit+'</b> sur l'+AP+'impact métier réel — écart corrigé.</div></div>' : '';
    var ownLabel=ownName(a.owner);
    var html='<div class="ai-card">'+
      '<div class="ai-top"><div class="ai-spark"><svg viewBox="0 0 24 24"><path d="M12 3v4M12 17v4M3 12h4M17 12h4M6 6l2.5 2.5M15.5 15.5 18 18M18 6l-2.5 2.5M8.5 15.5 6 18"/><circle cx="12" cy="12" r="3"/></svg></div>'+
      '<div><h3>Analyse IA</h3><p>'+(a.summary||'Analyse terminée.')+'</p></div></div>'+
      '<div class="axes">'+
        '<div class="axis crit"><div class="al"><svg class="ic" viewBox="0 0 24 24"><path d="M12 2 2 22h20L12 2Z"/><path d="M12 9v5M12 18h.01"/></svg>Criticité métier</div>'+
          '<span class="crit-pill '+a.crit+'">'+a.crit+'</span><div class="sub">'+(a.crit_reason||'')+'</div></div>'+
        '<div class="axis own"><div class="al"><svg class="ic" viewBox="0 0 24 24"><circle cx="12" cy="7" r="4"/><path d="M5 21a7 7 0 0 1 14 0"/></svg>Responsabilité</div>'+
          '<span class="own-tag">'+ownIcon(a.owner)+ownLabel+'</span><div class="sub">Qui agit — distinct de la criticité.</div></div>'+
      '</div>'+contrast+
      '<div class="tags"><span class="tag ci">'+catCI(a.ci)+'</span><span class="tag">⏱ ~'+(a.eta_minutes||30)+' min</span><span class="tag">Clôture probable : '+(a.close_code||'—')+'</span></div>'+
      '<div class="ai-fix"><h4><svg class="ic" viewBox="0 0 24 24"><path d="M9 11l3 3 8-8"/><path d="M20 12v6a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h9"/></svg>Essayez d'+AP+'abord ceci</h4>'+
        '<ol class="steps">'+(a.self_help||[]).map(function(s,i){return '<li><b>'+(i+1)+'</b><span>'+s+'</span></li>'}).join('')+'</ol></div>'+
      '<div class="ai-actions"><button class="solved" id="ai-solved">✓ Problème résolu</button>'+
        '<button class="escalate" id="ai-esc">Créer le ticket</button></div></div>';
    var r=$('#ai-result');r.innerHTML=html;r.scrollIntoView({behavior:'smooth',block:'nearest'});
    $('#ai-solved').onclick=function(){r.classList.add('hidden');$('#desc').value='';selCat=null;$$('.qcat').forEach(function(x){x.classList.remove('sel')});toast('Top — résolu sans ticket. Charge support évitée.')};
    $('#ai-esc').onclick=createTicket;
  }
  function createTicket(){
    var a=last;if(!a)return;
    var n='INC'+(13218000+MEM.filter(function(t){return t.mine}).length+1);
    var sameOpen=MEM.filter(function(t){return t.ci===a.ci&&t.status!=='resolved'&&!t.mine})[0];
    var tk={id:n,ci:a.ci,store:'4012',city:'TROYES',dr:'DR15',
      short:(lastDesc.length>54?lastDesc.slice(0,54)+'…':lastDesc),desc:lastDesc,
      snPrio:a.sn_prio||'P3',crit:a.crit,critReason:a.crit_reason||'',owner:a.owner,
      status:'open',close:'',sla:Date.now()+(a.eta_minutes||60)*60000,
      rec:1+MEM.filter(function(t){return t.ci===a.ci}).length,
      dup:sameOpen?sameOpen.id:null,t:Date.now(),mine:true,
      ai:(a.summary||'')+' '+(a.crit_reason||'')+' Geste conseillé : '+((a.self_help||[]).join(' / '))};
    MEM.push(tk);save();
    $('#ai-result').classList.add('hidden');$('#desc').value='';selCat=null;
    $$('.qcat').forEach(function(x){x.classList.remove('sel')});
    renderMine();renderTech();renderDash();updateBadge();
    toast('Ticket '+n+' créé · criticité '+tk.crit+' · '+ownName(tk.owner)+(tk.dup?' · doublon signalé':''));
  }

  $('#btn-analyze').onclick=function(){
    var d=$('#desc').value.trim();
    if(d.length<6){toast('Décrivez le problème en quelques mots.');$('#desc').focus();return}
    lastDesc=d;lastLoc=$('#store-loc').value;
    var b=$('#btn-analyze');b.disabled=true;b.querySelector('.ba-text').textContent='Analyse…';
    showSkeleton();
    analyze(d,selCat).then(function(a){
      b.disabled=false;b.querySelector('.ba-text').textContent='Analyser';showAnalysis(a);
    });
  };

  var tf='open',RANK={P1:0,P2:1,P3:2,P4:3};
  function renderTech(){
    var list=MEM.slice();
    if(tf==='open')list=list.filter(function(t){return t.status!=='resolved'});
    else if(tf==='resolved')list=list.filter(function(t){return t.status==='resolved'});
    list.sort(function(a,b){return (a.status==='resolved')-(b.status==='resolved')||RANK[a.crit]-RANK[b.crit]||b.t-a.t});
    var open=MEM.filter(function(t){return t.status!=='resolved'}).length;
    $('#tech-sub').textContent=open+' actif'+(open>1?'s':'')+' · triés par criticité métier';
    var el=$('#tech-list');
    if(!list.length){el.innerHTML='<div class="empty">Aucun ticket dans cette vue.</div>';return}
    el.innerHTML=list.map(function(t){
      var done=t.status==='resolved';
      var diff=t.snPrio&&t.snPrio!==t.crit;
      var s=slaTxt(t.sla);
      return '<div class="tk '+(done?'done':'')+'"><div class="tk-rail '+t.crit+'"></div><div class="tk-body">'+
        '<div class="tk-row"><span class="tk-id">'+t.id+' · '+ago(t.t)+'</span>'+
          (done?'':'<span class="tk-sla '+(s.w?'warn':'')+'">'+s.t+'</span>')+'</div>'+
        '<h4>'+t.short+'</h4>'+
        '<div class="loc-line">SPM '+t.store+' · '+t.city+' · '+t.dr+' — '+catCI(t.ci)+'</div>'+
        '<div class="tk-axes">'+
          '<span class="cbadge '+t.crit+'">'+t.crit+' <small>criticité</small></span>'+
          '<span class="obadge">'+ownIcon(t.owner)+ownName(t.owner)+'</span>'+
          (diff?'<span class="diffchip">⚠ système : '+t.snPrio+'</span>':'')+
        '</div>'+
        '<div class="tk-meta">'+
          (t.rec>1?'<span class="chip rec">⟳ Récurrent ×'+t.rec+'</span>':'')+
          (t.dup?'<span class="chip dup">⧉ Doublon '+t.dup+'</span>':'')+
          (done&&t.close?'<span class="chip">'+t.close+'</span>':'')+
        '</div>'+
        '<div class="tk-ai"><div class="lab"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M12 3v3M12 18v3M3 12h3M18 12h3"/></svg>Préconisation IA · base de connaissance</div>'+
          (t.ai||'—')+'</div>'+
        (done?'<div class="tk-act"><button disabled style="opacity:.6">✓ Clos — '+(t.close||'résolu')+'</button></div>'
         :'<div class="tk-act">'+(t.status==='open'?'<button class="prog" data-prog="'+t.id+'">Prendre en charge</button>':'')+
           '<button class="res" data-res="'+t.id+'">Marquer résolu</button></div>')+
      '</div></div>';
    }).join('');
    $$('[data-res]').forEach(function(b){b.onclick=function(){
      var t=MEM.filter(function(x){return x.id===b.dataset.res})[0];
      t.status='resolved';t.close=t.close||(t.owner==='dr'||t.owner==='elec'?'Non-IT Reason':'Software / Application Issue');
      save();renderTech();renderMine();renderDash();updateBadge();toast(t.id+' clos — interruption stoppée.');
    }});
    $$('[data-prog]').forEach(function(b){b.onclick=function(){
      var t=MEM.filter(function(x){return x.id===b.dataset.prog})[0];
      t.status='progress';save();renderTech();renderMine();updateBadge();toast(t.id+' pris en charge — '+ownName(t.owner)+'.');
    }});
  }
  $$('#tech-filter .seg-btn').forEach(function(b){b.onclick=function(){
    $$('#tech-filter .seg-btn').forEach(function(x){x.classList.remove('active')});
    b.classList.add('active');tf=b.dataset.f;renderTech();
  }});

  function renderDash(){
    var open=MEM.filter(function(t){return t.status!=='resolved'}).length;
    var gaps=MEM.filter(function(t){return t.snPrio&&RANK[t.crit]<RANK[t.snPrio]});
    var dups=MEM.filter(function(t){return t.dup}).length;
    $('#kpi-grid').innerHTML=
      '<div class="kpi"><div class="kv">'+open+'</div><div class="kl">Incidents actifs</div><div class="kt down">priorisés par impact</div></div>'+
      '<div class="kpi"><div class="kv">'+gaps.length+'</div><div class="kl">Mispriorisations corrigées</div><div class="kt up">↑ vs système</div></div>'+
      '<div class="kpi"><div class="kv">31<small style="font-size:13px">min</small></div><div class="kl">MTTR moyen</div><div class="kt up">↓ −42 % avec l'+AP+'IA</div></div>'+
      '<div class="kpi"><div class="kv">'+dups+'</div><div class="kl">Doublons détectés</div><div class="kt up">charge support évitée</div></div>';
    var gl=$('#gap-list');
    if(!gaps.length){gl.innerHTML='<div class="empty">Aucun écart détecté.</div>'}
    else gl.innerHTML=gaps.slice(0,4).map(function(t){
      return '<div class="gap-row"><div class="gx"><strong>'+t.short+'</strong><span>'+t.id+' · SPM '+t.store+' · '+catName(t.ci)+'</span></div>'+
        '<div class="prio-move"><span class="p-old">'+t.snPrio+'</span><svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:14px;height:14px;color:var(--ink-2)"><path d="M5 12h14M13 6l6 6-6 6"/></svg><span class="p-new">'+t.crit+'</span></div></div>';
    }).join('');
    var by={};MEM.forEach(function(t){by[t.ci]=(by[t.ci]||0)+1});
    var rows=Object.keys(by).map(function(k){return [k,by[k]]}).sort(function(a,b){return b[1]-a[1]});
    var mx=Math.max.apply(null,rows.map(function(r){return r[1]}).concat([1]));
    $('#bars').innerHTML=rows.map(function(r){
      return '<div class="bar-row"><span class="bn">'+catName(r[0])+'</span><span class="bar-track"><span class="bar-fill" data-w="'+Math.round(r[1]/mx*100)+'"></span></span><span class="bv">'+r[1]+'</span></div>';
    }).join('');
    requestAnimationFrame(function(){requestAnimationFrame(function(){
      $$('#bars .bar-fill').forEach(function(f){f.style.width=f.dataset.w+'%'});
    })});
  }
  $('#btn-predict').onclick=function(){
    var b=$('#btn-predict');b.disabled=true;b.textContent='Analyse…';
    $('#predict-body').innerHTML='<div class="ai-think" style="padding:6px 0"><span class="spin"></span>Croisement récurrence × résolutions × événements connexes…</div>';
    aiPredict().then(function(p){AI_ON=true;return p}).catch(function(){AI_ON=false;return localPredict()}).then(function(p){
      netUI();b.disabled=false;b.textContent='Relancer';
      $('#predict-body').innerHTML='<p class="muted" style="margin-bottom:4px">'+p.risks.length+' équipements à risque identifiés :</p>'+
        p.risks.map(function(r){return '<div class="risk '+r.level+'"><div class="rl"></div><div style="flex:1">'+
          '<div class="rt">'+r.equipment+'<em>'+(r.probability||'')+' '+(r.level==='high'?'· élevé':r.level==='med'?'· moyen':'· faible')+'</em></div>'+
          '<div class="rd">'+r.reason+'<br><b>Action :</b> '+r.action+'</div></div></div>';}).join('');
    });
  };

  var ROLE={employee:'Espace magasin',tech:'Espace support',dash:'Pilotage IA'};
  function go(v){
    $$('.view').forEach(function(s){s.classList.toggle('hidden',s.dataset.view!==v)});
    $$('.tab').forEach(function(t){t.classList.toggle('active',t.dataset.go===v)});
    $('#role-label').textContent=ROLE[v];$('#screen').scrollTop=0;
    if(v==='tech')renderTech();if(v==='dash')renderDash();
  }
  $$('.tab').forEach(function(t){t.onclick=function(){go(t.dataset.go)}});
  function updateBadge(){var n=MEM.filter(function(t){return t.status==='open'}).length;var b=$('#tab-badge');b.textContent=n;b.classList.toggle('show',n>0)}

  load().then(function(){renderCats();renderMine();renderTech();renderDash();updateBadge();netUI()});
})();
</script>
